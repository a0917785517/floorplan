# CLAUDE.md — 室內平面圖繪圖慣例

本檔記錄本專案的繪圖慣例，**所有 session 都必須遵循**。修改 `floorplan.drawio`
前請先讀完本檔。

## 1. 座標與比例

- 格式：draw.io（diagrams.net）的 mxGraph XML，副檔名 `.drawio`。
- 比例：**1 px = 1 cm**，座標原點在**左上角**，x 向右、y 向下。
- 尺寸換算：畫布上 100 px = 100 cm = 1 m。
- 目前總體範圍：700 cm × 700 cm。

## 2. 牆體

- **牆厚 15 cm**。
- 牆體以「一般 rect + 粗邊框」模擬：房間用矩形，`strokeWidth=15`（= 15 cm），
  邊框置中於房間標稱邊界，向內外各延伸 7.5 cm。
- 也可改用 `shape=mxgraph.floorplan.wallU` 等 floorplan 圖形庫樣式。
- 相鄰房間共用一面牆：兩個房間矩形的相鄰邊重合，15 px 邊框置中於該邊界線上，
  即代表同一道 15 cm 共用牆。

## 3. 門

- 使用 draw.io 內建 floorplan 圖形庫：`shape=mxgraph.floorplan.doorRight` 或
  `doorLeft`，必須畫出開門弧線。
- 以 `direction`（north/south/east/west）控制開門方向，讓弧線朝房間內部。
- 門的 bounding box：寬 = 門寬，另一邊 = 迴轉半徑（通常等於門寬）；
  貼齊所在牆面，`offset` 由該房間該面牆的起點量起。

## 4. 圖層結構

固定使用四個圖層（mxCell 直接掛在 `id="0"` 之下，順序如下）：

| 圖層 id        | value（名稱） | 內容                         |
| -------------- | ------------- | ---------------------------- |
| `layer_rooms`  | `rooms`       | 房間矩形（牆體）             |
| `layer_doors`  | `doors`       | 門與開門弧線                 |
| `layer_labels` | `labels`      | 房名 / 尺寸 / 面積文字標籤   |
| `layer_dims`   | `dims`        | 尺寸標註（總體與隔間尺寸線） |

每個圖形 `parent` 指向所屬圖層 id，不要混層。

## 5. 標籤格式

每個房間放一個文字標籤，置於房間中央，三行內容：

```
房名
寬 × 高 m
面積 m² (坪數 坪)
```

- 尺寸以 **公尺（m）** 表示（畫布 px ÷ 100）。
- 面積 = 寬(m) × 高(m)，單位 **m²**，取小數 2 位。
- 坪數 = 面積(m²) ÷ **3.30579**，取小數 2 位。

範例：`客廳 / 4.2 × 3.8 m / 15.96 m² (4.83 坪)`

## 6. 修改格局的規則

1. **同步更新標籤**：移動任何一面牆而改變到房間尺寸時，必須同步更新該房間
   （及所有受影響房間）在 `labels` 圖層的尺寸、m²、坪數，以及 `dims` 圖層的
   尺寸標註。數字對不上視為錯誤。
2. **共用牆連動**：移動一面牆時，檢查隔壁房間是否需要對應調整。相鄰房間共用
   同一邊界座標——一邊改了，另一邊要跟著改，**避免出現重疊或縫隙**。
   （例：主臥右牆右移，次臥左牆必須同步右移並縮小次臥寬度。）
3. **驗證 XML**：每次修改後都要確認 XML well-formed 才能 commit：
   ```
   xmllint --noout floorplan.drawio
   # 或
   python3 -c "import xml.etree.ElementTree as ET; ET.parse('floorplan.drawio')"
   ```
   **絕對不要推送會讓 draw.io 打不開的壞檔。**

## 7. Commit 訊息

- 一律用**繁體中文**描述格局變更，聚焦「改了什麼房間、怎麼改」。
- 範例：
  - `主臥加寬至 3.8m，次臥對應縮小至 1.7m`
  - `衛浴門改到左牆，避開廚房動線`
  - `餐廳與客廳之間新增隔間牆`

---

## 8. 倉庫平面圖限制（`warehouse-8f.drawio`）

本章為 **8 樓倉庫** 專用限制，與上面住宅平面圖是兩套不同圖。

**⚑ 倉庫圖採「宣告式管線」，不要手改 `warehouse-8f.drawio` 的 XML。** 事實來源是
`warehouse.yaml`；流程為：

```
改 warehouse.yaml → python3 generate.py → python3 validate.py → draw.io 開檔驗收
```

- `warehouse.yaml`：**單一事實來源**（尺寸旋鈕、走道寬、門位、固定物座標、45 個品項的
  數量/顏色/●數）。改一個尺寸，`generate.py` 會**反算整張圖**、不會跑版。
- `generate.py`：由 yaml 從零重算 `warehouse-8f.drawio`；裝不下會**大聲失敗、不寫檔**。
- `validate.py`：讀 yaml＋drawio，把關本章全部限制＋「規格↔圖」交叉檢查；有 FAIL 以
  exit 1 結束，**不得 commit/push**。（舊命令 `python3 check_warehouse.py` 保留為薄殼，仍可用。）

**改完必須跑 `python3 validate.py` 全綠才能 commit/push。** 下面各節是 `validate.py`
把關的規則；數值型參數請改 `warehouse.yaml`，不要直接改 drawio。

### 8.1 尺寸與單位

- 總場地：**2640 × 1800 cm**。
- 棧板：**110 × 110 cm = 1 格**（畫布上約 **106 × 106 px 正方形**，沿用現有繪圖比例）。
  〔`validate.py` 把關：棧板/成品格須為 110×110cm 正方形〕

### 8.2 群組棧板（同品項）

- 同一品項的 **1 至多個棧板視為同一個「群組棧板」**。
- **同品項棧板須相鄰群聚（不可孤立）**：同一品項的每個棧板，**至少要有一邊**與**同品項的
  另一個棧板相鄰**（共用一條邊）；不可有落單、與同群其他板不相連的孤立棧板。整個群組
  應連成一塊（相鄰格構成的連通區）。`validate.py` 會逐板檢查，只要有任一板找不到
  同品項的相鄰板即 FAIL。
- 群組內**只需維持至少一個棧板能接觸至少一條走道**即可取料（其餘可往深處堆）。
- 走到深處 / 轉角才拿得到的棧板**不在此規範內**（不要求每一板都臨走道）。

### 8.3 走道

- **領料走道寬度 ≥ 130 cm**（**所有走道一律遵守，含最靠前/後門那一側牆的走道**）。
- **淡黃領料走道寬度一律「一致」**：所有淡黃領料走道的**淨寬須相同**（同一數值，目前統一
  ≈133 cm，一律 ≥130 cm）。**不可有任何一條（尤其門側那條）畫得比其他條明顯較寬或較窄**——
  即使尺寸都合規，寬窄不一會讓圖面看起來不一致（曾發生門側走道被畫成兩倍寬）。
  `validate.py` 會比對所有淡黃帶的短向淨寬，彼此差距超過約 12 cm 即 FAIL。
- **中央主幹道**：**淨空、不得排料**，且**寬度 ≥ 130 cm**。
- **走道不得與任一物件重疊**：走道底色**不可蓋在任何物件（棧板 / 柱 / B01 / 門…）之上**——
  重疊會讓走道**視覺上變窄**（雖尺寸合規但看起來不足）。柱等固定物若落在走道範圍，
  走道底色須**繞開留缺口**，不可覆蓋。
- **走道以底色標示，色號固定不可更動；走道上不得有任何物件（棧板等）擋住：**
  - **主幹道 = 淡藍 `#E3F2FD`**（邊框 `#1565C0`）。
  - **其他領料走道 = 淡黃 `#FFFDE7`**。
  - 柱等固定物若落在走道範圍內屬既有結構，動線須繞開，不算違規。

### 8.4 門

- **前門：完全不使用。**
- **後門：使用。** 門正前方須留**淨空作業區**（勿堆放）。〔尺寸待補：目前淨空深度暫定 **≈155 cm**（150 px），待確認〕
- 門厚（門在牆上占的深度方向）：〔尺寸待補：目前暫定 **≈44 cm**（42 px），待確認〕
- **門在牆上的精確位置**（沿南牆量測，牆界 = 2640 cm，左緣為 0）：
  - **後門**：寬 **180 cm**，**左端**位在牆壁**由左至右 912 cm** 處（即涵蓋 912–1092 cm）。
  - **前門**：寬 **180 cm**，**右端**位在牆壁**由右至左 125 cm** 處（即涵蓋 2335–2515 cm）。
  - 換算 px 時用倉庫比例 `PXCM = 106/110`（牆左緣 x=80、右緣 x=2624）；`validate.py`
    會回推每個門的沿牆位置與寬度，超出 ±10 cm 即 FAIL。
- **前 / 後門的進出位置一律不放物料**（門口正對區淨空）。
- **倉庫外走廊**：緊鄰使用中門的那面外牆之**牆外**，須畫一條**走廊通道**（底色 `#ECEFF1`、
  邊框 `#607D8B`、標示「走廊」）。**前 / 後門一律朝此走廊開**，門的擺放位置才符合邏輯
  （門是牆上開口，外接走廊、內接倉庫）。走廊屬**牆外**（與前室 / 備用箱同屬牆外，不受
  「場地 = 牆界」限制），且須**繞開備用箱等牆外物件**、不覆蓋其上。
  `validate.py` 會確認走廊存在且每個門都對得到走廊。**走廊淨寬** 目前 **≈133 cm**
  （與領料走道同寬，一律 ≥130 cm）〔長度沿牆；淨寬如需改為其他值請在此註記，待補〕。
- 行進動線須**避開門旁的柱**。

### 8.5 固定物（不可移動）

- **5 根柱**、**B01（固定）**、**置物架**。
- **柱尺寸固定 72 × 72 cm**（畫布約 69 × 69 px）。
- **B01（固定）尺寸**：〔尺寸待補：目前畫布約 **898 × 289 cm**（866 × 278 px），暫定、待確認實際尺寸〕。
- **置物架尺寸**：〔尺寸待補：目前畫布約 **70 × 149 cm**（67 × 144 px），暫定、待確認實際尺寸〕。
- **前室**＝前門與倉庫之間的空間（含手繪的**備用藍箱 / 備用綠箱**），及**前 / 後門位置**。
  **備用箱尺寸**：每個 **110 × 110 cm**（與棧板同，牆外）。
- 以上座標一律不得更動。
- 〔以上標「尺寸待補」者，請填入實際 cm；填好後只需改 **`warehouse.yaml`** 內 `fixed_objects`
  對應的 `w`/`h`（px），`generate.py` 與 `validate.py` 會自動以新尺寸生成與把關，無需改程式。〕
- **柱不得與任何棧板 / 物件重疊**：柱所在格位淨空，棧板須避開柱、繞排。
- **門側牆的柱貼牆朝內凸**：靠近前 / 後門**同一側牆**的柱，須**貼齊該面牆**（外緣切齊牆
  內側），並**朝倉庫內部凸出**，不可離牆內縮而在柱與牆之間留下無用夾縫；如此門側牆的
  領料走道才能取得足夠淨寬（見 8.3）。

### 8.6 成品預留區

- 目前保留 **70 板**（右側）。

### 8.7 標記

- **● = 不滿一板**（棧板右上角的黑點）。〔尺寸待補：目前 **≈19 × 19 cm**（18 px），純標示，暫定〕
- **前 / 後門用綠色**標示。

### 8.8 動線與擺放

- **高頻料**（醫療彩盒 / 包裝材…）要**靠近後門**。
- 同品項相鄰群聚。

### 8.9 圖面規範（重疊 / 上下層 / 外牆）

- **不得重疊**：棧板彼此、棧板與柱 / B01 / 置物架**不得互相重疊**（走道底色、● 標記、
  文字標籤這類「附著」元素例外）。
- **上下層明確**：物件重疊時堆疊順序必須明確——**固定物（柱、B01）、● 不滿一板、
  文字標籤一律置於最上層**（在 XML 中排在被覆蓋物件之後），不可被棧板蓋住。
- **外牆明確**：倉庫**外圍邊緣**須畫出一個明確的矩形牆框（**2640 × 1800 cm**），
  包住全部內容，作為場地邊界。**外牆厚度**：〔尺寸待補：目前牆框線寬 **≈8 cm**（8 px），
  暫定、待確認（住宅圖的 15cm 牆厚不適用於倉庫）〕。
- **線條粗細一致**：線條可以重疊，但**同一類元素的邊框粗細必須一致**
  （所有貨棧板邊框同粗、所有成品格邊框同粗…），避免重疊處看起來忽粗忽細。
- **牆邊不得切物**：外牆邊緣**不得切到任何物件或空間**——所有內容必須完整落在
  牆框內側，不可有物件跨在牆線上（門為牆上開口，屬例外）。前室 / 備用箱 / 走廊屬牆**外**。
- **場地 = 牆界，內容不得超出**：**2640 × 1800 cm 即為外牆邊界**；場地內所有物件
  ＋走道 / 空間的範圍加總**不得超出**此尺寸。
- **走道不蓋牆框**：走道（淡黃 / 淡藍）**不得覆蓋到外牆邊框線**，須與牆框留出間隙，
  以免牆框視覺上忽粗忽細。

### 8.10 驗證與生成流程

**標準流程（改格局）**：

```
# 1. 編輯 warehouse.yaml(尺寸/走道/門位/固定物/品項)
python3 generate.py                     # 由 yaml 重算 warehouse-8f.drawio(裝不下會失敗不寫檔)
xmllint --noout warehouse-8f.drawio     # XML well-formed
python3 validate.py                     # 全部限制+規格交叉檢查,有 FAIL 不得 commit
# 2. draw.io 開檔目視驗收
```

- `generate.py [輸出檔] [--config warehouse.yaml]`：預設輸出 `warehouse-8f.drawio`；
  可輸出到別的檔名先預覽（如 `python3 generate.py preview.drawio`）再決定是否覆蓋正式檔。
- `validate.py [檔] [--config warehouse.yaml]`：預設驗 `warehouse-8f.drawio`。
- 若只是要驗證現有檔（未動 yaml），直接 `python3 validate.py` 即可。
- **尺寸「待補」值（B01、置物架、外牆厚、門厚、門前淨空、●）現在都在 `warehouse.yaml`**：
  填入真實 cm/px 改一行，`generate.py` 與 `validate.py` 會同步生效，無需改程式。
